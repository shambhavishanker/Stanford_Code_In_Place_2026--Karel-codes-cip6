def main():

    curr_value = int(input("Enter a number: "))

#check is current value is less than 100, if true, curr_value is doubled and printed and the loop repeats
    while curr_value<100:
        curr_value=curr_value*2
        print(curr_value)

if __name__ == '__main__':
    main()
